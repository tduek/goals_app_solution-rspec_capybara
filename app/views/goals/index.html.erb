<h1>Goals index</h1>

<ol>
  <% @goals.each do |goal| %>
  <li>
    <%= link_to goal.title, goal %>
    <% if logged_in? && current_user.id == goal.user_id %>
    <%= render partial: "goals/mark_completed", locals: {goal: goal} %>
    <% end %>
  </li>
  <% end %>
</ol>